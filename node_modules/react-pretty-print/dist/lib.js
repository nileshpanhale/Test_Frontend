const React = require('react');

module.exports = React.createClass({displayName: "exports",
  render: function() {
    return (
      React.createElement("pre", null, JSON.stringify(this.props.children, null, 2))
    );
  }
});
